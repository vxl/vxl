To generate the configure file for vxl:
 cd $VXLROOT/config.cmake
 autoconf configure.in > ../configure
 chmod a+x ../configure

For an out-of-place build
 cd $VXLROOT
 mkdir ../output-directory
 cd ../output-directory
 $VXLROOT/configure
 make
