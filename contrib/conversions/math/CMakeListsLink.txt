LINK_LIBRARIES( vnl vbl )

IF(HAS_COOL)
  LINK_DIRECTORIES( COOL Numerics )
  LINK_LIBRARIES( TJCOOL )
ENDIF(HAS_COOL)

IF(HAS_NUMERICS)
  LINK_DIRECTORIES( Numerics )
  LINK_LIBRARIES( Numerics )
ENDIF(HAS_NUMERICS)

