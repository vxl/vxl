#include <vsrl/vsrl_intensity_token.h>
#include <vcl_vector.txx>

VCL_VECTOR_INSTANTIATE(vsrl_intensity_token*);

#include <vcl_algorithm.txx>
VCL_FIND_INSTANTIATE(vcl_vector<vsrl_intensity_token*>::iterator, vsrl_intensity_token*);
