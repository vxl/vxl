#include <vtol/vtol_one_chain.h>
#include <vcl_vector.txx>

VCL_VECTOR_INSTANTIATE(vtol_one_chain*);

#include <vcl_algorithm.txx>
VCL_FIND_INSTANTIATE(vcl_vector<vtol_one_chain*>::iterator, vtol_one_chain*);
