#include <vtol/vtol_two_chain.h>
#include <vcl_vector.txx>

VCL_VECTOR_INSTANTIATE(vtol_two_chain*);

#include <vcl_algorithm.txx>
VCL_FIND_INSTANTIATE(vcl_vector<vtol_two_chain*>::iterator, vtol_two_chain*);
