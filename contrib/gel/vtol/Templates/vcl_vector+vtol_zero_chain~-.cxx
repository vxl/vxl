#include <vtol/vtol_zero_chain.h>
#include <vcl_vector.txx>

VCL_VECTOR_INSTANTIATE(vtol_zero_chain*);

#include <vcl_algorithm.txx>
VCL_FIND_INSTANTIATE(vcl_vector<vtol_zero_chain*>::iterator, vtol_zero_chain*);
