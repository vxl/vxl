# ./mul/mil/algo/CMakeListsLink.txt

LINK_LIBRARIES( mil_algo )

IF (LIBRARY_OUTPUT_PATH MATCHES '^$')
  LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/mul/mil/algo )
ENDIF (LIBRARY_OUTPUT_PATH MATCHES '^$')

# Immediate dependencies
INCLUDE( ${allvxl_SOURCE_DIR}/mul/mil/CMakeListsLink.txt )
