#
# ./oxl/CMakeListsLink.txt
#
#   geoff.cross@crd.ge.com
#

LINK_LIBRARIES(osl osl_internals mvl)

LINK_DIRECTORIES(
${allvxl_BINARY_DIR}/oxl/mvl
${allvxl_BINARY_DIR}/oxl/osl
${allvxl_BINARY_DIR}/oxl/osl/internals
)

INCLUDE(${CMAKE_SOURCE_DIR}/config.cmake/Modules/FindOpenGL.cmake)

IF(HAS_OPENGL)
  INCLUDE(${allvxl_SOURCE_DIR}/oxl/vgui/CMakeListsLink.txt)
ENDIF(HAS_OPENGL)
