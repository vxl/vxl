# ./oxl/vgui/CMakeListsLink.txt

LINK_LIBRARIES( vgui )

IF (LIBRARY_OUTPUT_PATH MATCHES '^$')
  LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/oxl/vgui )
ENDIF (LIBRARY_OUTPUT_PATH MATCHES '^$')

# add stuff to use MFC in this executable
IF(HAS_MFC)
 ADD_DEFINITIONS(-D_AFXDLL)
 SET(CMAKE_MFC_FLAG 6)
ENDIF(HAS_MFC)

# Immediate dependencies
INCLUDE( ${allvxl_SOURCE_DIR}/oxl/vgui/impl/CMakeListsLink.txt )
INCLUDE( ${CMAKE_SOURCE_DIR}/config.cmake/Modules/FindGLUT.cmake )
INCLUDE( ${CMAKE_SOURCE_DIR}/config.cmake/Modules/FindOpenGL.cmake )
INCLUDE( ${CMAKE_SOURCE_DIR}/config.cmake/Modules/FindX11.cmake )
INCLUDE( ${CMAKE_SOURCE_DIR}/config.cmake/Modules/FindQv.cmake )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vnl/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vnl/algo/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vil/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vgl/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vpl/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vbl/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vul/CMakeListsLink.txt )

# this is a temporary hack to get vgui on the link line again
# this is needed for Cygwin/gcc
# this will go away when cmake 1.4 link line dependency features are used
LINK_LIBRARIES( vgui )
