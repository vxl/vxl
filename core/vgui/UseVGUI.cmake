SET( HAS_VGUI "NO" )
IF(BUILD_VGUI)
  INCLUDE(${MODULE_PATH}/FindOpenGL.cmake)
  IF(HAS_OPENGL)
    INCLUDE_DIRECTORIES( ${OPENGL_INCLUDE_PATH} )
    SET( HAS_VGUI "YES" )
  ENDIF(HAS_OPENGL)
ENDIF(BUILD_VGUI)
