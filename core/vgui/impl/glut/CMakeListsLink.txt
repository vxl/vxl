LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/oxl/vgui/impl/glut )
LINK_LIBRARIES( vgui_glut )

LINK_DIRECTORIES( ${GLUT_LIBRARY_PATH} )
LINK_LIBRARIES( ${GLUT_LIBRARY} )

IF(X11_LIBRARY)
  LINK_DIRECTORIES( ${X11_LIB_PATH} )
  LINK_LIBRARIES( ${X11_LIBRARY} )
ENDIF(X11_LIBRARY)

