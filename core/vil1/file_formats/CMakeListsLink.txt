LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/vxl/vil/file_formats )
LINK_LIBRARIES( vil_file_formats )

INCLUDE(${CMAKE_SOURCE_DIR}/config.cmake/Modules/FindJPEG.cmake)

IF(NATIVE_JPEG_LIBRARY)
  LINK_DIRECTORIES( ${NATIVE_JPEG_LIB_PATH} )
  LINK_LIBRARIES( ${NATIVE_JPEG_LIBRARY} )
ELSE(NATIVE_JPEG_LIBRARY)
  INCLUDE( ${allvxl_SOURCE_DIR}/v3p/jpeg/CMakeListsLink.txt )
ENDIF(NATIVE_JPEG_LIBRARY)


IF(NATIVE_PNG_LIBRARY)
  LINK_DIRECTORIES( ${NATIVE_PNG_LIB_PATH} )
  LINK_LIBRARIES( ${NATIVE_PNG_LIBRARY} )
ELSE(NATIVE_PNG_LIBRARY)
  INCLUDE( ${allvxl_SOURCE_DIR}/v3p/png/CMakeListsLink.txt )
ENDIF(NATIVE_PNG_LIBRARY)


IF(NATIVE_TIFF_LIBRARY)
  LINK_DIRECTORIES( ${NATIVE_TIFF_LIB_PATH} )
  LINK_LIBRARIES( ${NATIVE_TIFF_LIBRARY} )
ELSE(NATIVE_TIFF_LIBRARY)
  INCLUDE( ${allvxl_SOURCE_DIR}/v3p/tiff/CMakeListsLink.txt )
ENDIF(NATIVE_TIFF_LIBRARY)
