# ./vxl/vil/io/CMakeListsLink.txt

LINK_LIBRARIES( vil_io )

IF (LIBRARY_OUTPUT_PATH MATCHES '^$')
  LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/vxl/vil/io )
ENDIF (LIBRARY_OUTPUT_PATH MATCHES '^$')

# Immediate dependencies
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vil/CMakeListsLink.txt )
INCLUDE( ${allvxl_SOURCE_DIR}/vxl/vsl/CMakeListsLink.txt )
