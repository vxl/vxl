# ./v3p/jpeg/CMakeListsLink.txt

LINK_LIBRARIES( jpeg )

INCLUDE_DIRECTORIES( ${allvxl_SOURCE_DIR}/v3p/jpeg )

IF (LIBRARY_OUTPUT_PATH MATCHES '^$')
  LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/v3p/jpeg )
ENDIF (LIBRARY_OUTPUT_PATH MATCHES '^$')
