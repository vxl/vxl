# ./v3p/tiff/CMakeListsLink.txt

LINK_LIBRARIES( tiff )

INCLUDE_DIRECTORIES( ${allvxl_SOURCE_DIR}/v3p/tiff )

IF (LIBRARY_OUTPUT_PATH MATCHES '^$')
  LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/v3p/tiff )
ENDIF (LIBRARY_OUTPUT_PATH MATCHES '^$')
