# ./v3p/zlib/CMakeListsLink.txt

LINK_LIBRARIES( zlib )

INCLUDE_DIRECTORIES( ${allvxl_SOURCE_DIR}/v3p/zlib )

IF (LIBRARY_OUTPUT_PATH MATCHES '^$')
  LINK_DIRECTORIES( ${allvxl_BINARY_DIR}/v3p/zlib )
ENDIF (LIBRARY_OUTPUT_PATH MATCHES '^$')
