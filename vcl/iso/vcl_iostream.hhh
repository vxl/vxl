#ifndef vcl_iso_iostream_h_
#define vcl_iso_iostream_h_

#include <iostream>

@cin
@cout
@cerr
@clog
// the following are not actually in the
// <iostream> synopsis:
@ios
@ios::in
@ios::out
@ios::ate
@ios::app
@ios::trunc
@ios::binary
@hex
@dec
@ws
@setprecision
@endl
@flush
@streampos
@streambuf
@streamsize

// allowed for now
using std::cin;
using std::cout;
using std::cerr;
using std::clog;
using std::endl;
using std::flush;

#endif
